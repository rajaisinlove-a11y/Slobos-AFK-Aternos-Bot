MC_HOST=your-java-server.example.com
MC_PORT=25565
MC_USERNAME=java_afk_bot
MC_PASSWORD=
MC_AUTH=offline
MC_VERSION=

PORT=5000

CHAT_MESSAGES=I am keeping the server online|AFK bot is active
CHAT_INTERVAL_SECONDS=120
ANTI_AFK=true

AUTO_AUTH=false
AUTO_AUTH_PASSWORD=

COMBAT=false

# Required to use /start, /stop and /logs. Generate a long random string,
# then call the endpoints with the header: Authorization: Bearer <token>
DASHBOARD_TOKEN=
